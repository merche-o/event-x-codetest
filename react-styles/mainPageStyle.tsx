import { createStyles, makeStyles, Theme } from "@mui/styles";

export const UseStyles = makeStyles((theme: Theme) =>
createStyles({
  gridItgem: {
    padding: "15px",
  },
  title:{
      paddingLeft: "15px"
  },
})
);